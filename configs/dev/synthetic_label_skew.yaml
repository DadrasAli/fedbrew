experiment:
  seed: 42
  output_dir: outputs/synthetic_label_skew
  use_run_subdir: false
  tags:
    - dev
    - synthetic
    - label_skew
    - local_sgd
    - mlp
  notes: "Twenty rounds on the label-skewed synthetic manifest."

server:
  strategy: fedavg
  participation_rate: 1
  metrics:
    - fit_loss
    - fit_accuracy

client:
  update_rule: local_sgd
  batch_size: 32
  learning_rate: 0.01
  momentum: 0.0
  weight_decay: 0.0
  nesterov: false
  learning_rate_schedule: constant
  min_learning_rate: 0.0
  metrics:
    - fit_loss
    - fit_accuracy

data:
  path: data/generated/synthetic_label_skew/manifest.json
  # The partition and the split ratios are the generator's, not this run's:
  # they live in data/configs/synthetic_label_skew.yaml and are recorded in
  # the manifest this path points at. Restating them here read to nobody.

model:
  name: mlp
  input_dim: 6
  hidden_dim: 12
  num_classes: 3

runtime:
  deterministic: true
  # Strict: raise on a nondeterministic kernel rather than warn once on
  # stderr. Every CUDA kernel this model uses is deterministic on this stack
  # (measured on an A100-40GB with torch 2.5.1 and CUDA 11.8, August 2026), so
  # this costs nothing and catches a future op that is not.
  deterministic_warn_only: false
  device: auto
  use_amp: false
  checkpointing:
    enabled: true
    interval: 1
    save_last: true
    save_best: true
    best_metric: val_accuracy_sample_weighted_avg
    keep_last: 2
    save_every_round: false
  performance:
    # NOT throughput-only: "highest" keeps every fp32 matmul at fp32's 24
    # mantissa bits, where "high" drops them to TF32's 10 or a bfloat16 pair's
    # ~16. torch's own default, written out rather than left implicit --
    # absent and "highest" are the same run but not the same document, and the
    # FEMNIST and OpenImage arms set "high", so a config started by copying
    # this one would otherwise change the arithmetic silently. Keep it
    # identical across every arm of a comparison.
    matmul_precision: highest
    cudnn_benchmark: false

defaults:
  global_rounds: 20
  local_iterations: 1
